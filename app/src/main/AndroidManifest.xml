<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guo.lab">

    <application
        android:name=".LabApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:configChanges="orientation|screenSize" -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".library.ReaderActivity" />

        <service
            android:name=".library.LibraryService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />
        <service
            android:name=".binderpool.BinderPoolService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />

        <activity android:name=".binderpool.BinderPoolClientActivity" />
        <activity android:name=".socket.ConversationActivity" />

        <service
            android:process=":remote"
            android:name=".socket.ConversationService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>